[pytest]
; logging options
log_cli = 1
log_level = WARNING
log_format = %(asctime)s %(name)s: %(levelname)s %(message)s
asyncio_mode = strict
markers =
    data_layer: Mark as a data layer related test.
