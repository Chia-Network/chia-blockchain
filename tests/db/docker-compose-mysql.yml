version: '3.7'
services:
  db:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=lensassamanissatoshi
      - MYSQL_DATABASE=full_node_db
      - MYSQL_USER=chia
      - MYSQL_PASSWORD=chia
    ports:
      - 33061:3306
    networks:
      - public
    volumes:
      - mysql_volume:/var/lib/mysql:rw
volumes:
  mysql_volume:
networks:
  public:
    driver: bridge
