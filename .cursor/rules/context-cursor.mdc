---
description: Auto-attach repository tooling context for cursor configuration
globs:
  - .cursor/**
---

# Repository Tooling Context Available

Consolidated repository tooling context exists at `.cursor/context/repo-tooling.md`.

Read it before broad source exploration. For context-routing work, also read `.cursor/context/INDEX.md` and the affected context docs.
