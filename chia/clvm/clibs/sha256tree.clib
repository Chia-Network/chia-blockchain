(
  (defun sha256tree (TREE)
        (if (l TREE)
            (sha256 2 (sha256tree (f TREE)) (sha256tree (r TREE)))
            (sha256 1 TREE)))

  (defun-inline tree_node_hash (atom) (sha256 1 atom))
  (defun-inline tree_branch_hash (left right) (sha256 2 left right))
)