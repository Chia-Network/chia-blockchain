; This is a "genesis checker" for use with cc.clvm.
;
; This checker allows new CATs to be created if they have a signature from a specific pubkey on their eve spend

; The pubkey is curried in, making this lineage_check program unique and giving the CAT it's uniqueness

(mod (
      PUBKEY
      Truths
      delta
      _
    )
    (include condition_codes.clvm)

    ; main
    (if delta
      (x)
      (list (list AGG_SIG_ME PUBKEY '$'))
    )
)
