(mod (royalty_addresss nft_id my_amount)

  (include condition_codes.clvm)

  ; main
  (list
    (list CREATE_COIN royalty_addresss my_amount)
    (list CREATE_PUZZLE_ANNOUNCEMENT royalty_addresss nft_id my_amount)
    (list ASSERT_MY_AMOUNT my_amount)
  )
)
