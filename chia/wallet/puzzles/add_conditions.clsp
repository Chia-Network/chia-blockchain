(mod (NEW_CONDITIONS INNER_PUZZLE inner_solution)
  (defun merge_list (list_a list_b)
    (if list_a
      (c (f list_a) (merge_list (r list_a) list_b))
      list_b
    )
  )

  (merge_list NEW_CONDITIONS (a INNER_PUZZLE inner_solution))
)