(mod (amount)

    (defun generate_conds (id pair_count)
        (if pair_count
            (c (list 66 36 (+ "hello" pair_count) id) (c (list 67 36 (+ "hello" pair_count) id) (generate_conds id (- pair_count 1))))
            0
        )
    )

    (defun loop_coins (id amount)
        (if amount
            (c (list id (c 1 (generate_conds id 512)) 123 (list 0 (list 1))) (loop_coins (+ id 1) (- amount 1)))
            ()
        )   
    )
    ; main
    (list (loop_coins 0x0101010101010101010101010101010101010101010101010101010101010101 amount))
)